import { createContext } from 'react';

export const ContentHeightContext = createContext(null);
export const ContentWidthContext = createContext(null);
export const HomeCoverContext=createContext(null)